<!--pages/about/about.wxml-->
<view wx:if="{{!userInfo}}">
  <view class="container1" bindtap="getUserProfile">
  <image src=" ../image/avatar.png" class="avatar">   </image>
   <text class="longinText">请授权登录~</text>
</view>
</view>
<view>
  <view class="container1">
  <image src="{{userInfo.avatarUrl}}" class="avatar">   </image>
   <text class="name">{{userInfo.nickName}}</text>
   <text bindtap="loginout" class="text" wx:if="{{userInfo}}">退出登录</text>
</view>

<!-- <button bindtap="getUserProfile" 
  type="primary"
  wx:if="{{!userInfo}}"
  >授权登录
</button> -->
</view>


<view wx:if="{{userInfo}}">
<view class="item" bindtap="orderGo">
<text>我的订单</text>
<view class="right-arrow"></view>

</view>

<view class="item" bindtap="addressGo">

<text>地址设置</text>
<view class="right-arrow"></view>
</view>
<view class="item" bindtap="xxgo">

<text>用户服务协议</text>
<view class="right-arrow"></view>
</view>
</view>
